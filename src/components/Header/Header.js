import React from 'react';
// Components
import Navigation from './Navigation/Navigation';

const Header = () => {
  return (
    <div>
      <Navigation />
    </div>
  );
};

export default Header;
